{
  "name": "lambda-tests",
  "version": "1.0.0",
  "description": "test scripts for lambda functions",
  "scripts": {
    "pretest": "NODE_ENV=test jest --verbose __tests__/db.test.ts",
    "test": "NODE_ENV=test jest --verbose --rootDir=__tests__ --runInBand --testTimeout=5000"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "aws-lambda": "^1.0.6",
    "jest": "^27.0.6",
    "pg": "^8.6.0",
    "sequelize": "^6.6.5",
    "sqlite3": "^5.0.2",
    "ts-jest": "^27.0.4",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5"
  },
  "dependencies": {}
}
