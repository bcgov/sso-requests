import styled from 'styled-components';
import { HELP_TEXT_FONT_SIZE, SECONDARY_FONT_COLOR } from 'styles/theme';

export default styled.p`
  font-size: ${HELP_TEXT_FONT_SIZE};
  color: ${SECONDARY_FONT_COLOR} !important;
`;
